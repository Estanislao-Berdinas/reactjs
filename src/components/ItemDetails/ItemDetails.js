export default function ItemDetails () {
    
    // en el Item, cargar solo nombre e id y hacer que el boton me despligue información complementaria desde el itemdetailscontainer

    //dar un formato distinto a la card de bootstrap

    //itemlistcontainer = al details, promesa, usestate, useffect y dentro traer el componente itemdetails

    // recibe el objeto por prop, de un solo objeto en particular ->imgen titulo descripcion y precio
    
    return (
        <div>
            <img/>
            <span>Id:</span>
            <span>Nombre</span>
            <span>Apellido</span>
            <span>Descripcion</span>
            <span>Precio</span>

        </div>
    );
    

}